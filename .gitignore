# Dependencies
node_modules/

# Environment Variables & Secrets (CRITICAL)
.env
.env.local
.env.*
*.pem
*.key

# Agent Memory, State, and Databases (CRITICAL)
data/
*.sqlite
*.sqlite-journal
.instance.lock

# Build outputs
dist/
build/
*.tsbuildinfo

# Shared caches (fastembed model weights, etc.) — repo-local, not per-bot,
# populated by scripts/postinstall-prewarm.cjs.
.cache/

# OS Files
.DS_Store
Thumbs.db
